const blogPosts = document.getElementById('blogPosts');

        window.onload = () => {
            blogPosts.style.opacity = '1'; // Fade in effect
            blogPosts.style.transform = 'translateY(0)'; // Move into view
        };